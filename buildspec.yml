version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18

  build:
    commands:
      # run build script
      - npm install --force
      - npm run-script build

artifacts:
  files:
    - "*"
  base-directory: .next
    - package.json
# artifacts:
#   files: "**/*"
# artifacts:
#   files:
#     - .elasticbeanstalk/**/*
#     - build/**/*
#     - public/**/*
#     - src/**/*
#     - package.json
#     - appspec.yml
#     - buildspec.yml
#     - scripts/**/*
