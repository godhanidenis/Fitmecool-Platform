version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install --force
    build:
      commands:
        - npm install --force @img/sharp-linux-x64
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
